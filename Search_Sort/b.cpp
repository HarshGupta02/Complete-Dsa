#include<bits/stdc++.h>
using namespace std;
int main(){
    string digits;
    cin >> digits;
    int n;
    cin >> n;
    vector<string> passwords(n);
    for(int i = 0;i  <n ; i ++){
        cin >> passwords[i];    
    }
    return 0;
}